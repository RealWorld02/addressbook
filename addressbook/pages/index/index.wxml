<!--index.wxml-->
<view class="container">

  <view  wx:for="{{userID}}" >
    {{item.id}}  {{item.name}}
  </view>

  <view>
  <button bindtap='bindtest'>test</button>
  </view>
<view>{{mapp}}</view>
  <input name="uname" bindinput="usernameInput" placeholder="用户名" auto-focus/>

</view>
